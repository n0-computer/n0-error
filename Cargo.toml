[package]
name = "n0-error"
version = "0.1.0"
edition = "2024"

[dependencies]
n0-error-macros = { path = "n0-error-macros" }
anyhow = { version = "1", optional = true }

[dev-dependencies]
anyhow = "1"

[features]
default = ["anyhow"]
anyhow = ["dep:anyhow"]
